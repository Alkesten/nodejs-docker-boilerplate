# Service API boilerplate for connecting services

## Build & Run

To start the webpack build with hotreload

```
NODE_ENV=development npm run build
```

While build is running serve the server

```
npm run start
```
